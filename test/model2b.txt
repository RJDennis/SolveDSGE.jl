# Test four --- Deterministic growth model with partial parameterization

states:
k, z
end

jumps:
c
end

shocks:
end

parameters:
β 
σ = 1.1
δ = 0.025
α = 0.30
ρ = 0.8
end

equations:
k(+1) = (1.0 - δ)*k + exp(z)*k^α - c
c^(-σ) = β*c(+1)^(-σ)*(1.0 - δ + α*exp(z(+1))*k(+1)^(α - 1.0))
z(+1) = ρ*z
end